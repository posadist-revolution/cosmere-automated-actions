import { InvestitureChangeFunc } from "..";

export var invToZeroMap: Map<string, InvestitureChangeFunc> = new Map<string, InvestitureChangeFunc>([]);

export var invFromZeroMap: Map<string, InvestitureChangeFunc> = new Map<string, InvestitureChangeFunc>([]);